package com.example.springcarrental.service;

import com.example.springcarrental.model.Car;
import com.example.springcarrental.model.CarDescription;

public interface CarService extends GenericService<Car, String> {
    //Car findFirstAvailable(CarDescription carDescription);
}